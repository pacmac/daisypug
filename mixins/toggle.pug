mixin toggle(opts)
  - opts = opts || {}
  - const cls = ['dp-toggle', 'toggle', opts.color && `toggle-${opts.color}`, opts.size && `toggle-${opts.size}`, opts.class].filter(Boolean).join(' ')
  input(type="checkbox" class=cls checked=opts.checked)&attributes(attributes)
