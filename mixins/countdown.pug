mixin countdown(opts)
  - opts = opts || {}
  - const cls = ['dp-countdown', 'countdown', opts.class].filter(Boolean).join(' ')
  span(class=cls)&attributes(attributes)
    if opts.value !== undefined
      span(style=`--value:${opts.value}`)
    block
